name: generate commit graph animation

on:
  # run automatically every 24 hours
  schedule:
    - cron: "0 */24 * * *" 

  # allow manual trigger from Actions tab to run any time
  workflow_dispatch:
  
  # run on every push to master branch
  push:
    branches: [ "master" ]

jobs:
  generate:
    permissions: 
      contents: write
    runs-on: ubuntu-latest
    timeout-minutes: 5
    
    steps:
      # generates a snake game from a github user (<github_user_name>) contributions graph, output a svg animation at <svg_out_path>
      - name: generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark

      # store build time (ISO 8601 format) for commit message
      - name: Store build timestamp
        run: echo "BUILD_TIME=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_ENV
          
      # push the content of <build_dir> to a branch
      # the content will be available at https://raw.githubusercontent.com/<github_user>/<repository>/<target_branch>/<file> , or as github page
      - name: push github-contribution-grid-snake *.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v4
        with:
          target_branch: output
          build_dir: dist
          author: Ryan Dsouza <dsouzarc@gmail.com>
          committer: Ryan Dsouza <dsouzarc@gmail.com>
          commit_message: Github Contributions as of ${{ env.BUILD_TIME }}
          verbose: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
